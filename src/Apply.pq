/**
 * Take a function that accepts an arbitary number of arguments and
 * transform this to a function that takes a single list argument
 * containing the original function arguments.
 *
 * :: (*... -> a) -> [*] -> a
 */
Curry(Function.Invoke)
